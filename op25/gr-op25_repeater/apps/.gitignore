# Untracked files
.venv/
__pycache__/
.pytest_cache/
stderr*
*.2
*.blist
*.wlist
*.json
*.tsv
*.sh
*.txt
*.liq
*.bin
op25_python
# Talkgroup last-heard cache (tg_metadata.py). Written into the work dir, which
# is this directory for a standalone run. Safe to delete.
*.sqlite
*.sqlite-journal

# Tracked files that would otherwise be excluded
!op25.sh
!op25.liq
!example_liquidsoap.liq
!trunk.tsv
!cfg.json
!meta.json

!Palmetto800*